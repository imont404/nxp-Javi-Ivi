+++
type = "plan_log"
id = "student-windows-tool-bootstrap-2026-08-26T062141-0600"
plan_id = "student-windows-tool-bootstrap"
step_id = "offline-cache"
created = "2026-08-26T06:21:41-06:00"
+++

# Verified cache and explicit offline archives supported

Setup retains both exact archives under `out/downloads` and verifies a cached
archive before reuse. `-ArmArchive` and `-CoreToolsArchive` accept explicitly
supplied offline files while applying the same pinned SHA-256 and extracted
content checks. `.partial` downloads never become cache entries, and `-Force`
redownloads unless an explicit archive was supplied.

Added Windows fixture tests that install a valid local core-tools archive, prove
an idempotent second run, install from a preseeded cache whose URL cannot be
reached, reject an outer checksum mismatch, reject an inner manifest hash
mismatch, remove failed staging, and confirm both failures leave the known-good
runtime unchanged. All 16 focused setup contract and installer tests pass.

The real cache contains the 305,393,359-byte verified Arm archive and the
3,168,406-byte verified core-tools archive. A complete repeat setup took 1.34
seconds and performed no network transfer. The documented process-scoped
PowerShell execution-policy bypass also completed successfully without a
permanent machine policy change.
