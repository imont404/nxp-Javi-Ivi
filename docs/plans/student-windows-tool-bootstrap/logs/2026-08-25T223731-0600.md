+++
type = "plan_log"
id = "student-windows-tool-bootstrap-2026-08-25T223731-0600"
plan_id = "student-windows-tool-bootstrap"
step_id = "baseline-clean-export"
created = "2026-08-25T22:37:31-06:00"
+++

# Log: baseline-clean-export

Exported tracked `HEAD` files to
`C:\ELI\fit2026\nxp-cup-setup-baseline-20260825-222613\repo`, outside the source
repository so the duplicate plan catalog could not interfere with `dev-std`. Ran the
exported root `setup.ps1`, normal embedded build, normal host build, and host CLI self-test.

Setup completed successfully in 613.7 seconds. The Arm GNU 14.2.Rel1 archive was
305,393,359 bytes (305.4 MB decimal, 291.2 MiB), and the extracted repository-local
toolchain contained 1,223,880,151 file bytes. BITS provided no student-visible progress
during the download; extraction reported ten-percent increments. CMake, Ninja, uv, and
LLVM-MinGW were already visible from the workstation, so this proves relative-path and
repository-local Arm provisioning rather than a clean-machine installation of those tools.

The competition firmware built in 9.1 seconds. Its CMake cache resolved
`CMAKE_C_COMPILER` to the exported repository's
`out/toolchains/arm-gnu-toolchain-14.2.rel1-mingw-w64-i686-arm-none-eabi/bin/arm-none-eabi-gcc.exe`,
not the workstation MCUXpresso installation. The host viewer and CLI built in 8.8 seconds,
and `nxpc_tool.exe selftest` reported `selftest=ok`.

Published artifacts appeared only beneath the exported repository's `out/artifacts`:
the 4,953,368-byte AXF, 355,828-byte BIN, 1,450,496-byte CLI, 2,328,576-byte viewer,
2,510,336-byte SDL2 DLL, 2,664,960-byte pinned rblhost, and its 1,975-byte license.
The baseline therefore confirms current relative resolution and exposes the expected setup
download-size, silent-wait, and developer-tool over-installation issues for the next steps.
