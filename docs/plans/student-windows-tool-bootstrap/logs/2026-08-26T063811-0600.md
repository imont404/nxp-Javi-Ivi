+++
type = "plan_log"
id = "student-windows-tool-bootstrap-2026-08-26T063811-0600"
plan_id = "student-windows-tool-bootstrap"
step_id = "student-setup-docs"
created = "2026-08-26T06:38:11-06:00"
+++

# Authoritative student setup guide rewritten

Replaced the quarantined legacy content in `docs/setup.html` with one concise
Windows x64 setup, build, ROM-HID flash, viewer, and offline-archive workflow.
The guide uses only the current component commands and `out/artifacts` paths.
It documents the J11 normal path and the proven hold-SW3, tap-SW1/reset,
release-SW3 physical ROM entry sequence while keeping J-Link explicitly
optional.

Permission guidance is placed before the first setup operation. It recommends
only a process-scoped PowerShell execution-policy bypass, says not to weaken
machine-wide policy, distinguishes expected winget/UAC behavior, and routes
Group Policy, AppLocker, antivirus, or managed-winget blocks to the instructor
or IT staff. Additional warnings cover motor safety, charge-only USB cables,
disk space, proxies, interrupted downloads, checksum rejection, serial-port
ownership, multiple boards, missing optional J-Link, and offline CMake/Ninja
prerequisites.

The root README now begins with a prominent Start Here link and the four-command
student path, while referring all detailed permissions and recovery help to the
single setup guide. Added contract tests for the route, current commands and
artifacts, bounded permission advice, recovery controls, stale-command absence,
viewport metadata, and local-link resolution. The focused setup documentation
tests and four installer integrity tests pass. A full-page system-Chrome render
confirmed the local HTML/CSS layout.

The plan step remains pending until the active ROM flash/recovery step and its
clean-machine/board acceptance supply final evidence for documentation signoff.
