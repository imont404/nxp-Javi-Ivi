+++
type = "plan_log"
id = "student-windows-tool-bootstrap-2026-08-26T055900-0600"
plan_id = "student-windows-tool-bootstrap"
step_id = "publish-first-core-tools-release"
created = "2026-08-26T05:59:00-06:00"
+++

# First immutable core-tools release published

Pushed the preserved repository history and release implementation to the public
`wavenumber-eng/nxp_cup` repository without changing the local remote. Enabled
GitHub immutable releases before publication.

The first publication attempt stopped before creating a tag or release because
Windows PowerShell promoted the expected `gh release view` not-found diagnostic
to a terminating error. Commit `d87d3d13d8d47d13d637a22243c1f7f0b9e9137c`
made that negative existence check compatible with Windows PowerShell, and the
clean correction commit was pushed before retrying.

Ran:

```powershell
.\src\host\release.ps1 -Version 1.0.0 -Publish -Force
```

The native build, CLI self-tests, all eight Playwright tests, generated-page
drift checks, deterministic packaging, inner manifest verification, uploaded
draft download, and extracted-package self-test passed. The script then
published the draft and verified an anonymous download from the exact asset URL.

GitHub reports `core-tools-v1.0.0` as public, non-draft, non-prerelease, and
immutable, targeting the clean source commit above. The ZIP is 3,168,406 bytes
with SHA-256
`5aca14b2abfa497f5ca85e217ba8eca63c09a186f923d2a3d6a79535125d1652`.
The public asset URL is:

```text
https://github.com/wavenumber-eng/nxp_cup/releases/download/core-tools-v1.0.0/nxp-cup-core-tools-win-x64-1.0.0.zip
```
