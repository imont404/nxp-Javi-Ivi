+++
type = "plan_log"
id = "student-windows-tool-bootstrap-2026-08-26T062140-0600"
plan_id = "student-windows-tool-bootstrap"
step_id = "pinned-core-tools-install"
created = "2026-08-26T06:21:40-06:00"
+++

# Pinned core-tools installation implemented

Pinned immutable release `core-tools-v1.0.0`, asset URL, clean source commit
`d87d3d13d8d47d13d637a22243c1f7f0b9e9137c`, and outer SHA-256
`5aca14b2abfa497f5ca85e217ba8eca63c09a186f923d2a3d6a79535125d1652`
in `setup.versions.json`. Setup downloads the exact asset rather than resolving
`latest`, verifies the outer hash, extracts it into `out/staging`, checks the
release identity and exact manifest payload, verifies every file size and hash,
and runs `nxpc_tool.exe selftest` before replacing `out/artifacts/host`.

The existing host runtime remains in place until the complete candidate passes.
Directory switching retains a rollback copy and preserves the maintainer-only
`packages` directory. A matching installed manifest and self-test make ordinary
reruns skip download and extraction.

Validated against the real public GitHub asset. The first download and SHA-256
check passed, the nine-file flat archive extracted, the CLI reported
`selftest=ok`, and a repeat setup completed successfully without network access.
The installed manifest identifies version 1.0.0, Windows x64, and the pinned clean
release commit.
