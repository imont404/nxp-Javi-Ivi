+++
type = "plan_log"
id = "usb-debug-display-transport-2026-07-20T173638-0400"
plan_id = "usb-debug-display-transport"
step_id = "host-receiver-proof"
created = "2026-07-20T17:36:38-04:00"
+++

# Log: host-receiver-proof

Interactive WebSerial smoke test:

- User opened the browser viewer and reported visible magenta stripes.
- Interpretation: WebSerial permission, CDC open, START command, bulk receive, AVCU parsing, RGB565 conversion, canvas rendering, and live display path are all functioning at least at smoke-test level.
- The visible striping matches the current synthetic firmware limitation: the firmware initializes one 16 KiB payload pattern once and reuses that same payload for every frame offset. Since the host assembles chunks at different offsets, the rendered frame repeats the first chunk's color pattern as bands.

Next action: make the synthetic test payload offset-aware so each chunk contains the RGB565 pixels for its actual frame offset. Then rebuild/flash and recheck the browser image.
