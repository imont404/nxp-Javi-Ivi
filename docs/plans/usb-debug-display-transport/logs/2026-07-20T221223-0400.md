+++
type = "plan_log"
id = "usb-debug-display-transport-2026-07-20T221223-0400"
plan_id = "usb-debug-display-transport"
step_id = "avc-integration-slice"
created = "2026-07-20T22:12:23-04:00"
+++

# Log: avc-integration-slice

Added AVC_DBG_STATS_REPORT to the shared protocol and firmware camera stream. Camera mode now sends stats at stream start and every 24 completed frames; synthetic mode keeps periodic stats disabled so START_SYNTH remains a pure ceiling stress path. Updated native, Python, and WebSerial parsers to accept stats packets and expose firmware drop/busy/send-error counters. Built/flashed CMake firmware. Native camera 60.017 s: 2.870 MiB/s, 23.43 fps, 11248 frame chunks, 60 stats packets, zero dropped-before flags, zero malformed/sequence errors, firmware stats frames_dropped=0 endpoint_busy=0 send_error=0. Python camera 10.030 s: 2.868 MiB/s, 23.43 fps, 1878 chunks, 10 stats packets, zero errors and zero firmware drop/busy/send-error counters. Synthetic START_SYNTH still measured about 34.0 MiB/s but one 5 s native run showed a single Windows CDC/parser gap at the ceiling, so use camera-mode stats for integration health and use synthetic as a transport stress ceiling. WebSerial static files passed node --check and fetched from localhost via a temporary server; Chrome serial-port selection still requires manual user gesture.
