+++
type = "plan_log"
id = "usb-debug-display-transport-2026-07-20T171955-0400"
plan_id = "usb-debug-display-transport"
step_id = "usb-only-throughput-proof"
created = "2026-07-20T17:19:55-04:00"
+++

# Log: usb-only-throughput-proof

User confirmed the separate debug USB and HS USB bench setup is available. The CDC test project now has its own AGENTS.md, build.ps1, and flash.ps1 so it can be used like the active AVC project without relying on root wrappers.

Current bench state: stock NXP CDC VCOM image is flashed and running. Windows enumerates the HS USB device side as COM15 with VID_1FC9 PID_0094, and a basic COM15 echo check passed.

Next action: move beyond stock echo into the USB-only throughput proof by adding synthetic RGB565 frame streaming to the CDC test firmware and a host-side receiver/measurement tool.
