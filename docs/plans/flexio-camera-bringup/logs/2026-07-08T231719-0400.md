+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-08T231719-0400"
plan_id = "flexio-camera-bringup"
step_id = "signal-instrumentation"
created = "2026-07-08T23:17:19-04:00"
+++

# Log: signal-instrumentation

Added the first firmware checkpoint for camera capture backend selection. The
default build now explicitly selects SmartDMA/EZH, preserves the existing camera
path, and includes a dormant FlexIO diagnostic backend that initializes the
sensor and configures `P4_20`, `P4_21`, and `P4_22` as GPIO inputs when selected.

Verified `.\build_cmake.ps1`, flashed with `.\flash.ps1 -CMake`, and confirmed
RTT boot output including `Camera capture backend: SMARTDMA_EZH`. The
MCUXpresso headless wrapper did not return within two minutes on this run; its
stale process was stopped, and the CMake-flashed image is the current validated
board state.
