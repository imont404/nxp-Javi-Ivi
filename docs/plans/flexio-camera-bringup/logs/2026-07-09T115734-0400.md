+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-09T115734-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-09T11:57:34-04:00"
+++

# Log: flexio-low-rate-capture

Hardware handoff for the first FlexIO camera data-bus wiring pass.

The planned data wiring remains:

- Camera `D0` to `P4_12` / `FLEXIO0_D20`.
- Camera `D1` to `P4_13` / `FLEXIO0_D21`.
- Camera `D2` to `P4_14` / `FLEXIO0_D22`.
- Camera `D3` to `P4_15` / `FLEXIO0_D23`.
- Camera `D4` to `P4_16` / `FLEXIO0_D24`.
- Camera `D5` to `P4_17` / `FLEXIO0_D25`.
- Camera `D6` to `P4_18` / `FLEXIO0_D26`.
- Camera `D7` to `P4_19` / `FLEXIO0_D27`.

For this first pass, the data lines will be wired without added terminators.
The sync-line termination/pulldown treatment remains in place. Data-line
termination will be deferred unless later GPIO/FlexIO capture or LCD display
tests show visual symptoms such as random pixel noise, bit sparkle, or
edge-correlated artifacts.

After wiring is complete, the next firmware check should confirm that the
existing Port 4 sync diagnostic still reports plausible HSYNC, VSYNC, and PCLK
activity before attempting data-bit sampling.
