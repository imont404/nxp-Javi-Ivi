+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-10T142946-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-10T14:29:46-04:00"
+++

# Log: flexio-low-rate-capture

Resume attempt after the hardware pause.

Pre-checks:

- Working tree was clean.
- The `FLEXIO_PIPELINE_DIAG` CMake image was present at
  `build\cmake\avc_core0-Debug\avc_core0.axf`.
- The explicit SEGGER V9.54 J-Link executable was present.

Flash command:

```powershell
.\flash.ps1 -CMake -JLinkPath 'C:\Program Files\SEGGER\JLink_V954\JLink.exe'
```

Result:

The image was not flashed. J-Link connected to the onboard probe, but reported
no target voltage:

```text
VTref=0.000V
Target voltage too low.
```

Interpretation:

This is different from the previous `DM-AP IDCODE 0x00000000` failure. The
debug probe is visible over USB, but the MCXN947 target side is not powered, not
connected to VTref, or not exposing target voltage to the onboard J-Link.

Next action:

Check the FRDM debug USB-C connection, board power state, and any power/jumper
state that would feed target voltage to the onboard J-Link. Retry flash only
after J-Link reports a nonzero VTref, normally about `3.3 V`.
