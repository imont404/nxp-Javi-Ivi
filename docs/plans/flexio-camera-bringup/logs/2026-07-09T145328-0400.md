+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-09T145328-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-09T14:53:28-04:00"
+++

# Log: flexio-low-rate-capture

Hardware state changed to isolate the VSYNC problem.

The previous partial data-bus state had only `D0/D1` attached, but VSYNC still
reported thousands of false edges per second. All camera data lines are now
disconnected from the new Port 4 FlexIO data-bus wiring.

Current test state:

- `PCLK -> P4_20` remains fitted.
- `HSYNC/HREF -> P4_21` remains fitted.
- `VSYNC -> P4_22` remains fitted.
- `D0..D7 -> P4_12..P4_19` are disconnected.

Next action is to rerun the existing `FLEXIO_DIAG` RTT sync diagnostic. If
VSYNC remains noisy in this sync-only state, the issue is isolated to the sync
wiring or VSYNC input path rather than data-line coupling.
