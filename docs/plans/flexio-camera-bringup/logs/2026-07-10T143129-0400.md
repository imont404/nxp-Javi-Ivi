+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-10T143129-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-10T14:31:29-04:00"
+++

# Log: flexio-low-rate-capture

Successfully flashed and RTT-tested the `FLEXIO_PIPELINE_DIAG` image after the
debug connection was corrected.

Flash command:

```powershell
.\flash.ps1 -CMake -JLinkPath 'C:\Program Files\SEGGER\JLink_V954\JLink.exe'
```

Flash result:

- J-Link connected to serial `1066857635`.
- Target voltage reported `VTref=3.300V`.
- MCXN947 core connected over SWD at `4000 kHz`.
- Flash download programmed and verified successfully.

RTT command:

```powershell
.\rtt.ps1 -CMake -Reset -Seconds 15 -JLinkDllPath 'C:\Program Files\SEGGER\JLink_V954\JLink_x64.dll'
```

RTT result:

- Firmware booted into `Camera capture backend: FLEXIO_PIPELINE_DIAG`.
- Port 4 sync inputs configured as expected:
  `PCLK=P4_20`, `HSYNC/HREF=P4_21`, `VSYNC=P4_22`.
- The backend reported the active `320x200` line window as `180-220`.
- Synthetic frame handoff is working: `cam_pipe` frame deltas are steady at
  `+30` frames per report interval.
- Sync remains clean: `p4_lines=200`, PCLK activity is present, and the run had
  only one rejected VSYNC, likely the startup partial frame.

Representative RTT lines:

```text
cam_pipe frames=54(+54) reject=1 p4_vs=55(+55) p4_hs=10971 p4_lines=200 p4_pclk=1 next_buf=0 line_win=180-220
cam_pipe frames=84(+30) reject=1 p4_vs=85(+30) p4_hs=16986 p4_lines=200 p4_pclk=1 next_buf=0 line_win=180-220
cam_pipe frames=204(+30) reject=1 p4_vs=205(+30) p4_hs=40986 p4_lines=200 p4_pclk=1 next_buf=0 line_win=180-220
cam_pipe frames=624(+30) reject=1 p4_vs=625(+30) p4_hs=124986 p4_lines=200 p4_pclk=1 next_buf=0 line_win=180-220
```

Interpretation:

The sync-only software frame pipeline is proven from camera sync IRQs through
`avc__next_frame()` handoff. The next check is visual: confirm that the LCD
shows the synthetic RGB565 test pattern when the application is in test mode.
