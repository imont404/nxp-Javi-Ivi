+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-09T151007-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-09T15:10:07-04:00"
+++

# Log: flexio-low-rate-capture

Hardware pause checkpoint.

The FRDM-MCXN947 board has been powered down and disconnected for now. Stop
flash and RTT attempts until the board is intentionally reconnected and powered
back up.

Current firmware/software state:

- Commit `0818ea8` added the `FLEXIO_PIPELINE_DIAG` backend.
- The CMake image builds successfully at
  `build\cmake\avc_core0-Debug\avc_core0.axf`.
- That image has not been flashed yet because the last J-Link attempts failed
  to connect to the target debug port before the hardware pause.

Current hardware context to preserve:

- Sync mapping remains `PCLK -> P4_20`, `HSYNC/HREF -> P4_21`, and
  `VSYNC -> P4_22`.
- Keep VSYNC physically separated from the other camera signals when resuming.
- The last data-line state had only `D0 -> P4_12` and `D1 -> P4_13` restored;
  `D2..D7` remained disconnected.

Next resume action:

After the board is reconnected and powered, retry the CMake flash using the
explicit SEGGER V9.54 path. If SWD still reports `DM-AP IDCODE 0x00000000`,
perform a reset/power-cycle recovery before spending time on RTT or LCD output.
