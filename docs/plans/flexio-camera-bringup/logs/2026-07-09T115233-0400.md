+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-09T115233-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-09T11:52:33-04:00"
+++

# Log: flexio-low-rate-capture

Updated the bring-up plan after confirming the active OV5640 geometry and
current Port 4 signal state.

Findings:

- The active AVC camera configuration is `320x200` RGB565, not `320x240`.
- The display remains `320x240`, with a `320x40` overlay band and a `320x200`
  camera view.
- The latest `p4_lines=200` RTT result therefore matches the active camera
  frame height.
- The signal-count exit criterion is now met: `P4_21` HSYNC/HREF is stable at
  about 6040 edges/sec, `P4_22` VSYNC is stable at about 30 to 31 edges/sec,
  `P4_20` PCLK edge activity is present, and the VSYNC guard did not trip.

Plan updates:

- Marked `signal-instrumentation` done.
- Marked `flexio-low-rate-capture` active.
- Marked the `signal-counts` exit criterion met.
- Added `docs/research/MCXN947/flexio_camera_io_pin_map.md` as the living I/O
  record for camera wiring, mux roles, electrical treatment, and planned Port 4
  FlexIO data wiring.

Next firmware work should prepare for data-bus bring-up on `P4_12..P4_19`
before asking for another wiring power-down.
