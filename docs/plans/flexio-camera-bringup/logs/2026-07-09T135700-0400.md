+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-09T135700-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-09T13:57:00-04:00"
+++

# Log: flexio-low-rate-capture

Hardware state changed for a smaller incremental data-bus test.

The noisy full data-bus pass was backed out:

- Disconnected all camera data lines from the new Port 4 FlexIO data-bus
  wiring.
- Refitted sync wiring:
  - `PCLK -> P4_20`.
  - `HSYNC/HREF -> P4_21`.
  - `VSYNC -> P4_22`.
- Reattached only:
  - `D0 -> P4_12` / `FLEXIO0_D20`.
  - `D1 -> P4_13` / `FLEXIO0_D21`.

The next check is to rerun the existing `FLEXIO_DIAG` RTT sync diagnostic with
only `D0/D1` present. If VSYNC returns to about 30 to 31 edges/sec and
`p4_lines=200`, proceed with a small data-bit diagnostic or add the remaining
data lines incrementally.
