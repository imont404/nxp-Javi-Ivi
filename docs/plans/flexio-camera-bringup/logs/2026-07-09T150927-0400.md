+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-09T150927-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-09T15:09:27-04:00"
+++

# Log: flexio-low-rate-capture

Added the first sync-qualified synthetic-frame backend for the no-data-line
pipeline test.

Firmware changes:

- Added `CAMERA_CAPTURE_BACKEND_FLEXIO_PIPELINE_DIAG` and made it the default
  camera backend for the current test image.
- Kept camera XCLK, SCCB/I2C, reset, power-down, and sensor initialization
  active.
- Reused the Port 4 GPIO sync instrumentation: `P4_20` PCLK flag,
  `P4_21` HSYNC/HREF IRQ, and `P4_22` VSYNC IRQ.
- Filled the existing two-frame camera buffer with deterministic RGB565 test
  patterns.
- On each VSYNC, accepted a synthetic frame only when the measured HSYNC count
  was inside the `180-220` line window for the active `320x200` mode.
- Added a main-loop camera service hook that emits low-rate `cam_pipe` RTT
  status without printing from the GPIO IRQ.

Build command:

```powershell
.\build_cmake.ps1
```

Build result:

- CMake/Ninja build completed successfully.
- Output image: `build\cmake\avc_core0-Debug\avc_core0.axf`
- Size summary: `text=241344`, `data=516`, `bss=304004`, `dec=545864`.
- `FRAME_BUFFERS` usage remained `304520 B` of `384 KB`.

Flash attempts:

```powershell
.\flash.ps1 -CMake -JLinkPath 'C:\Program Files\SEGGER\JLink_V954\JLink.exe'
.\flash.ps1 -CMake -SpeedKHz 1000 -JLinkPath 'C:\Program Files\SEGGER\JLink_V954\JLink.exe'
```

Flash result:

The image was not flashed. J-Link detected the onboard probe and target voltage
(`VTref=3.315V`) but could not connect to the MCXN947 target at either
`4000 kHz` or `1000 kHz`. Both attempts reported:

```text
DMAP_REG_ID: DM-AP IDCODE detected: 0x00000000
ERROR: Wrong DM-AP IDCODE detected: 0x00000000
```

Next action:

Recover the SWD connection before testing RTT or LCD output for this image.
Start with a board reset or power cycle, then retry the same CMake flash command
with the explicit J-Link V9.54 path. If that still fails, try a lower SWD speed
or a connect-under-reset recovery sequence.
