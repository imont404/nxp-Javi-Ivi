+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-09T120727-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-low-rate-capture"
created = "2026-07-09T12:07:27-04:00"
+++

# Log: flexio-low-rate-capture

Ran the existing `FLEXIO_DIAG` RTT sync diagnostic after the first camera
data-bus wiring pass. No firmware changes were made for this check.

Command:

```powershell
.\rtt.ps1 -CMake -Reset -Seconds 12
```

Result:

- `P4_21` HSYNC/HREF still looks stable at about 6036 to 6042 rising
  edges/sec.
- `P4_20` PCLK still reports edge activity in each interval.
- `P4_22` VSYNC is no longer clean. It now reports about 59 to 79 rising
  edges/sec instead of the previous 30 to 31 edges/sec.
- `p4_lines` is unstable, sometimes near 200 and sometimes much lower, which
  indicates extra VSYNC edges are being counted between true frames.
- The VSYNC IRQ guard did not trip because the noise is below the current
  120 edges/sec guard threshold.

Representative RTT lines:

```text
cam_diag t_ms=3000 p4_hs=18116(+6041) p4_vs_irq=193(+67) p4_lines=200 p4_pclk=1 p4_vs_flag=0 p4_vs_off=0 ref_hs=0(+0) ref_vs=0(+0) ref_lines=0 ref_pclk=0 lvl_p4=000 lvl_ref=101 unexp=0/0
cam_diag t_ms=5000 p4_hs=30198(+6041) p4_vs_irq=336(+74) p4_lines=7 p4_pclk=1 p4_vs_flag=0 p4_vs_off=0 ref_hs=0(+0) ref_vs=0(+0) ref_lines=0 ref_pclk=0 lvl_p4=000 lvl_ref=101 unexp=0/0
cam_diag t_ms=8000 p4_hs=48316(+6040) p4_vs_irq=544(+79) p4_lines=17 p4_pclk=1 p4_vs_flag=0 p4_vs_off=0 ref_hs=0(+0) ref_vs=0(+0) ref_lines=0 ref_pclk=0 lvl_p4=000 lvl_ref=101 unexp=0/0
```

Interpretation:

The data-bus wiring pass appears to have disturbed the VSYNC signal path or
introduced enough coupling into `P4_22` to create false rising edges. Because
HSYNC/HREF and PCLK still look active, the next check should focus on the
physical `P4_22` VSYNC connection and nearby data-bus wiring before adding a
data-bit sampling diagnostic.
