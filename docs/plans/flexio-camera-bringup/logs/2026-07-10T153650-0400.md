+++
type = "plan_log"
id = "flexio-camera-bringup-2026-07-10T153650-0400"
plan_id = "flexio-camera-bringup"
step_id = "flexio-frame-buffer"
created = "2026-07-10T15:36:50-04:00"
+++

# Log: flexio-frame-buffer

Status checkpoint before choosing the next hardware/data validation.

Known-good state:

- Latest committed checkpoint: `9806c54 Add FlexIO eDMA black-frame capture`.
- Active backend: `FLEXIO_EDMA`.
- Sync wiring remains `PCLK -> P4_20`, `HSYNC/HREF -> P4_21`, and
  `VSYNC -> P4_22`.
- FlexIO data pins are configured on `P4_12..P4_19` as `FLEXIO0_D20..D27`.
- Camera data lines are still disconnected for the black-frame test and the
  data inputs are held low by external pulldown/termination.
- Build, flash, and RTT all passed after the eDMA implementation.

Measured result:

- `cam_dma done` advances at 30 frames/sec.
- `submit_err=0`, `cb_err=0`, and `timeout=0`.
- Sampled frame data is black: `first=0000,0000,0000,0000` and `sample_nz=0`.

Interpretation:

The current result proves the FlexIO timer/shifter path, the DMA1 request,
full-frame movement into the AVC camera buffers, and the existing frame handoff
for the all-zero bus condition. It does not yet prove data-bit order, RGB565
byte order, or real OV5640 data quality.

Next decision:

Pause before reconnecting the full data bus. Pick the next validation method
based on wiring risk and observability:

- Tie one selected data bit high after removing or overriding its pulldown.
- Restore a small real-data subset such as D0/D1 and watch `sample_nz`, first
  words, and LCD behavior.
- Add a firmware diagnostic that snapshots sampled words before overlay and
  line-processing code can modify the displayed buffer.
