+++
type = "plan_log"
id = "alternate-240x320-display-bringup-2026-07-10T195758-0400"
plan_id = "alternate-240x320-display-bringup"
step_id = "collateral-intake"
created = "2026-07-10T19:57:58-04:00"
+++

# Log: Imported Browser-Downloaded Display Collateral

Copied the newly downloaded BuyDisplay collateral from
`C:\Users\EliHughes\Downloads` into `docs/research/ER-TFT020-7/downloads`:

- `ER-TFT020-7_Datasheet.pdf`
- `ST7789.pdf`
- `DS-CST816S_DS_V1.3.pdf`
- `ER-CON22HT-1.pdf`
- `ER-TFT020-7_8051_Tutorial.zip`

Extracted the tutorial ZIP into
`docs/research/ER-TFT020-7/extracted/ER-TFT020-7_8051_Tutorial`.

Initial example-code findings:

- The ZIP contains `ER-TFT020-7_3SPI`, `ER-TFT020-7_4SPI`, and
  `ER-TFT020-7_8BIT` Keil C51 projects plus `ER-TFT020-7_Interfacing.pdf`.
- The `4SPI` driver uses `CS`, `RS` as data/command, `SCK`, `SDI`, and `RES`.
  This matches the current AVC SPI architecture well enough for a guarded SPI
  display-test mode.
- The `4SPI` vendor init enables TE with command `0x35` and parameter `0x00`.
- The `8BIT` example labels the interface `8080-8BIT` and uses an `_WR` strobe
  with `_RD`, which conflicts with the module datasheet's `6800 8-bit
  Parallel` wording. Treat the parallel interface as likely 8080-style until
  the interfacing PDF or schematic proves otherwise.
- Rendered `ER-TFT020-7_Interfacing.pdf` to a PNG for quick inspection.
- The interfacing schematic confirms 4-wire SPI straps as `IM2=VDD`, `IM1=VDD`,
  and `IM0=GND`. It maps `/WR(DC)` to D/C, `DC(SCL)` to serial clock, `CS` to
  active-low chip select, and `SDA` to serial data.
- The same schematic labels the parallel reference as `8080 Series I`.

Updated the plan and manifest so the core collateral is now local and the
remaining dev-board user guide/schematic downloads are tracked as supplementary
parallel-interface references rather than blockers for first SPI bringup.

Follow-up scope note:

- SPI is the only active implementation focus right now.
- EZH/bunny work is deferred until the SPI path is validated and the proper
  project-specific EZH/bunny build references are supplied.
