+++
type = "plan_log"
id = "alternate-240x320-display-bringup-2026-07-12T101126-0400"
plan_id = "alternate-240x320-display-bringup"
step_id = "spi-validation"
created = "2026-07-12T10:11:26-04:00"
+++

# Log: Flashed Normal AVC Frame Path for ER-TFT020-7

Built and flashed the normal AVC firmware path for the ER-TFT020-7 after the
three-band RGB SPI proof passed.

Programmed image:

- Artifact:
  `build/cmake/avc_core0-Debug-er-tft020-7/avc_core0.axf`
- Build flags from the CMake cache:
  `-DCONFIG__DISPLAY_PANEL=2`
- `CONFIG__DISPLAY_TEST_MODE` was not defined, so it used the default `0`.

Build result:

- CMake/Ninja build passed in `build/cmake/avc_core0-Debug-er-tft020-7`.
- Size report:
  `text=246804 data=516 bss=304148 dec=551468 hex=86a2c`.

Flash result:

- J-Link V9.54 connected to `MCXN947_M33_0` over SWD at `4000 kHz`.
- J-Link saw `VTref=3.300V`, programmed one `253952 bytes` flash range,
  verified successfully, reset, and released the core.

Next bench check:

- Bench observation confirmed the ER-TFT020-7 works with the normal AVC display
  path.
- Check frame pacing, tearing, and any visual artifacts in SPI mode.
