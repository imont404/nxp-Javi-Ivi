+++
type = "plan_log"
id = "alternate-240x320-display-bringup-2026-07-12T103802-0400"
plan_id = "alternate-240x320-display-bringup"
step_id = "parallel-ezh-strategy"
created = "2026-07-12T10:38:02-04:00"
+++

# Log: Updated Bunny Build And CMake Source Discovery

Updated AVC's vendored bunny_build copy toward upstream `main`
(`9c5e8d6`, source-refactor layout) as the first implementation slice for the
parallel LCD/EZH work.

Changes made:

- Imported the split upstream instruction/pseudo-instruction source layout into
  `src/common/bunny_build`.
- Intentionally did not import upstream `include/bunny_build__config.h`, so AVC
  continues to use `src/avc/avc_core0/source/avc_config/bunny_build__config.h`.
- Added an `E_END()` wrapper for the existing upstream `E__END()` helper so both
  spellings are available for bounded EZH command programs.
- Changed the scripted CMake generator so source discovery uses durable
  `.cproject` source roots/options and `.project` linked resources.
- Removed the generator dependency on transient MCUXpresso `Debug` makefiles.
  Those files are regenerated by MCUXpresso and must not be treated as source
  truth.
- Preserved the important `.cproject` file/folder optimization overrides, such
  as `source/main.c`, `source/avc__line_processor.c`,
  `source/avc_io/eGFX_Driver_ER-TFT020-3.c`, and `egfx/src`.

Verification:

- `.\build_cmake.ps1` passed.
- `cmake --build build\cmake\avc_core0-Debug --clean-first` passed.
- The clean build compiled `bunny_build__instr.c` and
  `bunny_build__psuedo_instr.c`.
