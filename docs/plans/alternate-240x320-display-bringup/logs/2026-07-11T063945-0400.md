+++
type = "plan_log"
id = "alternate-240x320-display-bringup-2026-07-11T063945-0400"
plan_id = "alternate-240x320-display-bringup"
step_id = "spi-bringup"
created = "2026-07-11T06:39:45-04:00"
+++

# Log: Flashed ER-TFT020-7 SPI Test Image

Programmed the FRDM-MCXN947 with the ER-TFT020-7 SPI display-test image before
the display breakout wiring step.

Programmed image:

- Artifact:
  `build/cmake/avc_core0-Debug-er-tft020-7-test/avc_core0.axf`
- Build flags from `CMakeCache.txt`:
  `-DCONFIG__DISPLAY_PANEL=2 -DCONFIG__DISPLAY_TEST_MODE=1`
- J-Link Commander:
  `C:\Program Files\SEGGER\JLink_V954\JLink.exe`
- Device:
  `MCXN947_M33_0`
- Interface:
  `SWD @ 4000 kHz`

Flash result:

- J-Link connected to the board, saw `VTref=3.300V`, programmed one flash range
  of `253952 bytes`, verified successfully, reset, and released the core.
- The firmware should now be running the isolated display-test loop. With no
  display connected, the useful bench check is to probe the existing LCD SPI
  pins for the repeated full-frame transfer once per second.

RTT note:

- A short PyLink RTT check was attempted with the V954 DLL override, but the
  monitor did not return before the shell timeout.
- The recent RTT monitor processes were stopped so the J-Link is left free for
  probing and power-down work.

Next bench step:

- Probe the LCD SPI signals, then power down and wire the ER-TFT020-7 breakout
  if the clock/data/control pins look correct.
