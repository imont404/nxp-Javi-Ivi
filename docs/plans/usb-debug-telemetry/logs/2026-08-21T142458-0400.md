+++
type = "plan_log"
id = "usb-debug-telemetry-2026-08-21T142458-0400"
plan_id = "usb-debug-telemetry"
step_id = "realistic-validation"
created = "2026-08-21T14:24:58-04:00"
+++

# Log: realistic-validation

Automated CDC boundary testing observed zero bytes after open-only and legacy START traffic, then exposed a bulk-OUT endpoint re-arm fault on immediate framed reopen. Firmware now discards closed-session bytes, clears stale RX size when DTR drops, and immediately re-arms OUT. Python and native receivers now complete correlated SET_CHANNELS(0) and CLOSE handshakes before closing. Two immediate Python reopen runs and framed native camera/synthetic runs completed with zero parser, sequence, drop, busy, or send errors; synthetic reached 34.472 MiB/s. RTT showed the physical input currently selected TEST, so race-waiting hardware observation and physical unplug/replug remain open rather than being inferred.
